## .NET build output
bin/
obj/
[Dd]ebug/
[Rr]elease/
*.user

## Visual Studio
.vs/

## Packed .idea artifacts are build outputs of each component; the canonical
## copies the CMS ships live in /dist (committed). Per-project artifact/dist
## folders are not.
**/artifacts/
Themes/**/dist/
Plugins/**/dist/
Controls/**/dist/
Pages/**/dist/

# packed build outputs (regenerated by ma-idea pack; the CMS ships its own copy)
/dist/
